namespace NxEditor.Core.Localization;

public class StringResources_Statuses
{
    private const string GROUP = "Status";

    public readonly string Ready = GetStringResource(GROUP, nameof(Ready));
}
